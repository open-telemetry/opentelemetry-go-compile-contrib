client_hook:
  target: net/http
  where:
    func: RoundTrip
    recv: "*Transport"
  do:
    - inject_hooks:
        before: BeforeRoundTrip
        after: AfterRoundTrip
        path: "go.opentelemetry.io/otelc-contrib/instrumentation/net/http/client"
